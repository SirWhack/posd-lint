"""Should flag: comments paraphrasing the code they sit above."""


def double(n):
    # increment counter
    counter = 0
    # set duration to twice the input
    duration = n * 2
    # return the duration
    return duration


def process_user(user_id):
    # save the user_id to the database
    save_to_database(user_id)


def save_to_database(x):
    pass
